{
  "comment": "Every feature this fork carries over pingdotgg/t3code. Each entry's \"keep\" line is the per-file resolution rule an upstream merge conflict is settled by: what of ours must survive in the listed files, and what takes upstream. scripts/check-fork-features.ts fails when a keep line, a listed file or a test file is gone, then runs the listed tests. .github/workflows/fork-update.yml runs it in the gate, so an upstream merge that deletes a fork seam stops before it can publish. See docs/agents/upstream-conflict-resolution.md and docs/operations/fork-windows-build.md.",
  "features": [
    {
      "name": "fork-app-identity",
      "description": "The fork installs and runs beside official T3 Code: its own app id, product name, state directory (~/.t3-fork) and t3code-fork:// URL scheme. Issues #34 and #59.",
      "keep": "Our app id, product name, window title, ~/.t3-fork state directory, t3code-fork:// URL scheme and Linux desktop entry name and WM class replace upstream's wherever these files set them; take upstream for the rest of these files, then re-apply those settings on top.",
      "files": [
        "apps/desktop/src/app/DesktopEnvironment.ts",
        "apps/desktop/src/app/DesktopEarlyElectronStartup.ts",
        "apps/desktop/src/app/DesktopLinuxUrlHandler.ts",
        "apps/desktop/src/electron/ElectronProtocol.ts",
        "apps/desktop/src/app/DesktopEnvironment.test.ts",
        "apps/desktop/src/app/DesktopPreReadyPlatform.test.ts",
        "apps/desktop/src/window/DesktopWindow.test.ts"
      ],
      "test": "apps/desktop/src/app/DesktopLinuxUrlHandler.test.ts",
      "patchesUpstream": true
    },
    {
      "name": "fork-desktop-build",
      "description": "The desktop build stages the fork's app id, product name and installer file name, and reads its update source from T3CODE_DESKTOP_UPDATE_REPOSITORY. scripts/release-smoke.ts also carries the fork's fix for running the release-only shell steps under Git for Windows bash. Issues #59, #75 and #94.",
      "keep": "Our app id, product name, installer file name, the T3CODE_DESKTOP_UPDATE_REPOSITORY update source and the Git for Windows bash fix in release-smoke.ts replace upstream's; take upstream for the rest of these files, then re-apply those parts on top.",
      "files": ["scripts/build-desktop-artifact.ts", "scripts/release-smoke.ts"],
      "test": "scripts/build-desktop-artifact.test.ts",
      "patchesUpstream": true
    },
    {
      "name": "windows-console-suppression",
      "description": "Spawned server and provider processes no longer draw a console window on Windows. Issue #82.",
      "keep": "windowsConsole.ts is ours whole; take upstream for the rest of the other listed files, then re-apply every call into windowsConsole.ts that hides a spawned console window on top.",
      "files": [
        "packages/shared/src/windowsConsole.ts",
        "packages/shared/src/shell.ts",
        "apps/desktop/src/main.ts",
        "apps/server/src/bin.ts",
        "apps/server/src/serviceLauncher.ts"
      ],
      "test": "packages/shared/src/windowsConsole.test.ts",
      "patchesUpstream": true
    },
    {
      "name": "terminal-subprocess-poll",
      "description": "Bounded terminal and preview polling on one shared back-off engine: a probe budget held under the poll period, bounded fan-out, a round that backs off when nothing changed and returns to the base period on any event. Issue #83, merged in #106, re-applied on upstream v0.0.39's terminal manager in #125. pollLoop.ts is the fork's own module and upstream has no file at that path, so its test cannot pass on plain upstream.",
      "keep": "pollLoop.ts is ours whole, and Manager.ts and PortScanner.ts must still drive their polling through it; take upstream for the rest of those two files, then re-apply the pollLoop call sites on top.",
      "files": [
        "apps/server/src/pollLoop.ts",
        "apps/server/src/terminal/Manager.ts",
        "apps/server/src/preview/PortScanner.ts"
      ],
      "test": "apps/server/src/pollLoop.test.ts",
      "patchesUpstream": true
    },
    {
      "name": "fork-automatic-updates",
      "description": "A found update downloads with no click and installs on the next quit. One update pill label names the waiting version, Settings shows the state in plain words, and a switch turns it off for upstream's two-click flow. forkAutomaticUpdates.ts and forkAutomaticUpdates.logic.ts are the fork's own modules and upstream has no file at either path, so the test cannot pass on plain upstream. Issue #113.",
      "keep": "forkAutomaticUpdates.ts and forkAutomaticUpdates.logic.ts are ours whole, and the automatic download, the single update pill label, the Settings state and switch, and the IPC channel that carries them survive in the other listed files; the \"Update pill label\" row in docs/internals/glossary.md is ours whole too; everything else in those files takes upstream.",
      "files": [
        "apps/desktop/src/updates/forkAutomaticUpdates.ts",
        "apps/desktop/src/updates/DesktopUpdates.ts",
        "apps/desktop/src/updates/updateMachine.ts",
        "apps/desktop/src/updates/updatesTestHarness.ts",
        "apps/desktop/src/updates/DesktopUpdates.test.ts",
        "apps/desktop/src/settings/DesktopAppSettings.ts",
        "apps/desktop/src/ipc/channels.ts",
        "apps/desktop/src/ipc/methods/updates.ts",
        "apps/desktop/src/ipc/DesktopIpcHandlers.ts",
        "apps/desktop/src/preload.ts",
        "packages/contracts/src/ipc.ts",
        "apps/web/src/components/forkAutomaticUpdates.logic.ts",
        "apps/web/src/components/forkAutomaticUpdates.logic.test.ts",
        "apps/web/src/components/sidebar/SidebarUpdatePill.tsx",
        "apps/web/src/components/settings/SettingsPanels.tsx",
        "docs/internals/glossary.md"
      ],
      "test": "apps/desktop/src/updates/forkAutomaticUpdates.test.ts",
      "patchesUpstream": true
    },
    {
      "name": "fork-repository-identity-read-path",
      "description": "A project's repository identity is resolved off the read path and stored on its projection row, so a snapshot read never runs git. Issue #72.",
      "keep": "RepositoryIdentityReactor.ts, its service, the 003 fork migration and GitWorkDepth.ts are ours whole, and ProjectionSnapshotQuery.ts must keep serving the stored repository_identity_json column instead of resolving an identity on the read path; take upstream for the rest of ProjectionSnapshotQuery.ts, then re-apply the stored-column reads on top.",
      "files": [
        "apps/server/src/orchestration/Layers/RepositoryIdentityReactor.ts",
        "apps/server/src/orchestration/Services/RepositoryIdentityReactor.ts",
        "apps/server/src/persistence/ForkMigrations/003_ProjectionProjectRepositoryIdentity.ts",
        "apps/server/src/vcs/GitWorkDepth.ts",
        "apps/server/src/orchestration/Layers/ProjectionSnapshotQuery.ts"
      ],
      "test": "apps/server/src/orchestration/Layers/ShellSnapshotSpawn.test.ts",
      "patchesUpstream": true
    },
    {
      "name": "fm-acp-provider",
      "description": "First Mate is driven through its ACP door as a configurable ACP agent provider. Issues #78 and #79.",
      "keep": "Every listed file is the fork's own and upstream has no file at these paths, so keep our side of anything that conflicts here; nothing in these files takes upstream. If upstream adds a file at one of these paths, keep our file and rename upstream's.",
      "files": [
        "apps/server/src/provider/acpAgent/AcpAgentProvider.ts",
        "apps/server/src/provider/acpAgent/AcpAgentDriver.ts",
        "apps/server/src/provider/acpAgent/AcpAgentAdapter.ts",
        "apps/server/src/provider/acpAgent/AcpAgentIdentity.ts",
        "apps/server/src/provider/acpAgent/AcpAgentSupport.ts"
      ],
      "test": "apps/server/src/provider/acpAgent/AcpAgentProvider.test.ts",
      "patchesUpstream": false
    },
    {
      "name": "fm-worker-threads",
      "description": "First Mate workers show up as read-only threads, with the fork's own migration track behind them. Issue #81.",
      "keep": "AcpAgentWorkerThreadReactor.ts, AcpAgentWorkerSessions.ts, AcpPeerSessions.ts and ForkMigrations.ts are ours whole, and decider.ts must keep our read-only thread handling; take upstream for the rest of decider.ts, then re-apply the read-only rules on top.",
      "files": [
        "apps/server/src/provider/acpAgent/AcpAgentWorkerThreadReactor.ts",
        "apps/server/src/provider/acpAgent/AcpAgentWorkerSessions.ts",
        "apps/server/src/provider/acp/AcpPeerSessions.ts",
        "apps/server/src/persistence/ForkMigrations.ts",
        "apps/server/src/orchestration/decider.ts"
      ],
      "test": "apps/server/src/orchestration/decider.readOnly.test.ts",
      "patchesUpstream": true
    },
    {
      "name": "fork-connect-public-env",
      "description": "The desktop build ships with T3 Connect switched on. A clone with no .env gets .env.example copied to .env before the build spawns, so server.asar and app.asar carry the public Clerk key and relay URL. fork-public-env.ts is the fork's own module and upstream has no file at that path, so its test cannot pass on plain upstream. Issue #137.",
      "keep": "fork-public-env.ts is ours whole, and the call that copies .env.example to .env before the build spawns survives in build-desktop-artifact.ts, the workflow and the doc; everything else in those files takes upstream.",
      "files": [
        "scripts/lib/fork-public-env.ts",
        "scripts/build-desktop-artifact.ts",
        ".github/workflows/fork-update.yml",
        "docs/operations/fork-windows-build.md"
      ],
      "test": "scripts/lib/fork-public-env.test.ts",
      "patchesUpstream": true
    },
    {
      "name": "fm-worker-thread-model",
      "description": "A read-only worker thread says which model and thinking level is behind it: a strip where the composer would sit, and badges on the sidebar row. ReadOnlyThreadModel.tsx and readOnlyThreadModel.logic.ts are the fork's own modules and upstream has no file at either path, so the test cannot pass on plain upstream. Issue #135.",
      "keep": "ReadOnlyThreadModel.tsx and readOnlyThreadModel.logic.ts are ours whole, and the model and thinking level strip and sidebar badges they add survive in ChatView.tsx and Sidebar.tsx; everything else in those files takes upstream.",
      "files": [
        "apps/web/src/components/chat/ReadOnlyThreadModel.tsx",
        "apps/web/src/components/chat/readOnlyThreadModel.logic.ts",
        "apps/web/src/components/ChatView.tsx",
        "apps/web/src/components/Sidebar.tsx"
      ],
      "test": "apps/web/src/components/chat/readOnlyThreadModel.test.tsx",
      "patchesUpstream": true
    },
    {
      "name": "fm-fleet-thread-roles",
      "description": "Fleet threads carry a role. The First Mate thread never auto-settles and, pinned, has its own sidebar slot; fm-fleet-sidebar-tree arranges second mates and workers.",
      "keep": "fleetThreads.ts, useStartFirstMateThread.ts and fork migration 8 are ours whole, and the fleetRole and fleetRepo fields, the First Mate sidebar slot, the settlement exception and the single First Mate invariant survive in the files listed; everything else in those files follows upstream.",
      "files": [
        "packages/contracts/src/orchestration.ts",
        "packages/client-runtime/src/fleetThreads.ts",
        "apps/server/src/orchestration/commandInvariants.ts",
        "apps/server/src/orchestration/ThreadSettlementPolicy.ts",
        "apps/server/src/persistence/ForkMigrations/008_ProjectionThreadsFleetRole.ts",
        "apps/web/src/components/Sidebar.tsx",
        "apps/web/src/hooks/useStartFirstMateThread.ts"
      ],
      "test": "apps/server/src/orchestration/decider.fleetRole.test.ts",
      "patchesUpstream": true
    },
    {
      "name": "fm-fleet-sidebar-tree",
      "description": "The sidebar shows First Mate's fleet as a tree above the other threads. First Mate always holds the top, above a divider: the pinned First Mate, or else the newest live one, with any others under it; with none live, a Start First Mate row stands there instead. Second mates follow in repository-name order and never move with activity; each worker is indented under the second mate that shares its fleetRepo, snoozed ones included, and a second mate can fold its workers away. A settled worker leaves the tree and lists in the Settled section with the other settled threads, and un-settling it puts it back under its second mate. A snoozed fleet row, or a settled second mate, stays a full row in the tree, dimmed, with its wake or un-settle control, and a pinned second mate keeps its unpin. Fleet rows name their role on the model line in their own colour, then the model and reasoning level, with a ship, sailboat or pickaxe beside the project icon. A second mate's colour, and its workers' quieter one, comes from its project icon as the sidebar shows it: a badge's own colour, or an image logo's main colour read once from the image, with only its lightness set for each theme. Only an icon with no usable colour falls back to a stable colour from the repository name. First Mate keeps its pink.",
      "keep": "fleetSidebar.logic.ts, fleetTone.logic.ts, FleetTone.tsx, their tests, FirstMateIcon.tsx and FleetRoleIcon.tsx are ours whole. In Sidebar.tsx the fleet tree rows, the isSettled option passed to buildFleetTree that sends a settled worker to the Settled section, the FleetRowPlacement prop and onPin on SidebarThreadRow, the role line, the fold arrow, the First Mate slot with its Start First Mate row and divider survive; partitionFirstMateThreads survives in Sidebar.logic.ts and its tests in Sidebar.logic.test.ts; the First Mate threads section of docs/user/thread-sidebar.md is ours. Everything else in those files takes upstream.",
      "files": [
        "apps/web/src/components/sidebar/fleetSidebar.logic.ts",
        "apps/web/src/components/sidebar/fleetTone.logic.ts",
        "apps/web/src/components/sidebar/FleetTone.tsx",
        "apps/web/src/components/FirstMateIcon.tsx",
        "apps/web/src/components/FleetRoleIcon.tsx",
        "apps/web/src/components/Sidebar.tsx",
        "apps/web/src/components/Sidebar.logic.ts",
        "apps/web/src/components/Sidebar.logic.test.ts",
        "docs/user/thread-sidebar.md"
      ],
      "test": [
        "apps/web/src/components/sidebar/fleetSidebar.logic.test.ts",
        "apps/web/src/components/sidebar/fleetTone.logic.test.ts"
      ],
      "patchesUpstream": true
    },
    {
      "name": "fm-fleet-thread-read-only-clear",
      "description": "The fleet can clear read-only on a second mate's thread with thread.read-only.clear, so the user can type into it. Only the fleet issuer may send it, and only on a fleet-owned, unarchived thread whose fleetRole is second-mate. It records a thread.read-only-cleared event that sets readOnly to false on the thread and its shell row, so it survives a restart and clients update. Nothing makes a thread read-only again. The server advertises fleetThreadReadOnlyClear in its environment capabilities.",
      "keep": "fleetThreadReadOnlyClear.test.ts is ours whole. The thread.read-only-cleared case survives in threadReducer.test.ts, and the fleetThreadReadOnlyClear assertion survives in ServerEnvironment.test.ts. The thread.read-only.clear command, the thread.read-only-cleared event and payload, and the OrchestrationThread.readOnly comment survive in orchestration.ts; the fleetThreadReadOnlyClear capability survives in environment.ts and ServerEnvironment.ts; readOnlyClearRefusal survives in fleetThreads.ts; the issuer stamp for the command survives in Normalizer.ts; the command case survives in decider.ts; the event case survives in projector.ts, ProjectionPipeline.ts and threadReducer.ts; and the payload re-export survives in Schemas.ts. Everything else in those files takes upstream.",
      "files": [
        "packages/contracts/src/orchestration.ts",
        "packages/contracts/src/environment.ts",
        "packages/client-runtime/src/state/threadReducer.ts",
        "apps/server/src/environment/ServerEnvironment.ts",
        "apps/server/src/orchestration/fleetThreads.ts",
        "apps/server/src/orchestration/Normalizer.ts",
        "apps/server/src/orchestration/decider.ts",
        "apps/server/src/orchestration/projector.ts",
        "apps/server/src/orchestration/Schemas.ts",
        "apps/server/src/orchestration/Layers/ProjectionPipeline.ts"
      ],
      "test": [
        "apps/server/src/orchestration/fleetThreadReadOnlyClear.test.ts",
        "packages/client-runtime/src/state/threadReducer.test.ts",
        "apps/server/src/environment/ServerEnvironment.test.ts"
      ],
      "patchesUpstream": true
    },
    {
      "name": "fm-fleet-thread-berth-set",
      "description": "The fleet can set fleetRole and fleetRepo on a thread it already owns with thread.fleet-berth.set, to repair threads created before the fleet sent them. Only the fleet issuer may send it, only on a fleet-owned, unarchived thread that is not the First Mate thread, and never with fleetRole first-mate, so only thread.create makes a First Mate thread. It records a thread.fleet-berth-set event that sets fleet_role and fleet_repo on the thread and its shell row, so it survives a restart and clients update. The berth a thread already has is accepted and keeps its updatedAt. The server advertises fleetThreadBerthSet in its environment capabilities.",
      "keep": "fleetThreadBerthSet.test.ts is ours whole. The thread.fleet-berth-set case survives in threadReducer.test.ts, the fleetThreadBerthSet assertion survives in ServerEnvironment.test.ts, and the fleet berth set test survives in server.test.ts. The thread.fleet-berth.set command, the thread.fleet-berth-set event and payload, and the OrchestrationThread.fleetRole comment survive in orchestration.ts; the fleetThreadBerthSet capability survives in environment.ts and ServerEnvironment.ts; resolveFleetBerth survives in fleetThreads.ts; the issuer stamp for the command survives in Normalizer.ts; the command case survives in decider.ts; the event case survives in projector.ts, ProjectionPipeline.ts and threadReducer.ts; the payload re-export survives in Schemas.ts; and the thread.fleet-berth.set sentence of the Fleet role row survives in docs/internals/glossary.md. Everything else in those files takes upstream.",
      "files": [
        "packages/contracts/src/orchestration.ts",
        "packages/contracts/src/environment.ts",
        "packages/client-runtime/src/state/threadReducer.ts",
        "apps/server/src/environment/ServerEnvironment.ts",
        "apps/server/src/orchestration/fleetThreads.ts",
        "apps/server/src/orchestration/Normalizer.ts",
        "apps/server/src/orchestration/decider.ts",
        "apps/server/src/orchestration/projector.ts",
        "apps/server/src/orchestration/Schemas.ts",
        "apps/server/src/orchestration/Layers/ProjectionPipeline.ts",
        "docs/internals/glossary.md"
      ],
      "test": [
        "apps/server/src/orchestration/fleetThreadBerthSet.test.ts",
        "packages/client-runtime/src/state/threadReducer.test.ts",
        "apps/server/src/environment/ServerEnvironment.test.ts",
        "apps/server/src/server.test.ts"
      ],
      "patchesUpstream": true
    },
    {
      "name": "fm-turn-start-when-busy",
      "description": "A turn start can ask to wait for an idle thread with whenBusy: \"queue\", so a supervisor's wake never steers into a turn the person just started. The decider holds it while the thread is busy, starts it in the command that frees the thread after a normal turn end, and drops it with a recorded reason on Stop, a stopped, interrupted or failed session, a failed turn start, archive or delete. It is persisted, so it survives a restart and runs once. The server advertises turnStartWhenBusy in its environment capabilities.",
      "keep": "deferredTurnStarts.ts, ProjectionThreadDeferredTurnStarts.ts, fork migration 9 and turnStartWhenBusy.integration.test.ts are ours whole. The whenBusy option, the thread.turn-start-deferred and thread.deferred-turn-start-dropped events, the thread.deferred-turn-start.release command and the deferredTurnStarts and pendingTurnStart fields survive in orchestration.ts; the turnStartWhenBusy capability survives in environment.ts and ServerEnvironment.ts; the whenBusy queue branch, the release command case and the decideOrchestrationCommand wrapper that calls followDeferredTurnStarts survive in decider.ts; the projectEvent wrapper that calls projectDeferredTurnStarts survives in projector.ts; the deferred-turn-start table writes and reads survive in ProjectionPipeline.ts and ProjectionSnapshotQuery.ts; the startup release loop survives in serverRuntimeStartup.ts; and holdCompletion, getSteers, getTurnInputs and awaitSends survive in TestProviderAdapter.integration.ts. Everything else in those files takes upstream.",
      "files": [
        "packages/contracts/src/orchestration.ts",
        "packages/contracts/src/environment.ts",
        "apps/server/src/environment/ServerEnvironment.ts",
        "apps/server/src/orchestration/deferredTurnStarts.ts",
        "apps/server/src/orchestration/decider.ts",
        "apps/server/src/orchestration/projector.ts",
        "apps/server/src/orchestration/Layers/ProjectionPipeline.ts",
        "apps/server/src/orchestration/Layers/ProjectionSnapshotQuery.ts",
        "apps/server/src/serverRuntimeStartup.ts",
        "apps/server/src/persistence/ProjectionThreadDeferredTurnStarts.ts",
        "apps/server/src/persistence/ForkMigrations/009_ProjectionThreadDeferredTurnStarts.ts",
        "apps/server/integration/TestProviderAdapter.integration.ts"
      ],
      "test": "apps/server/integration/turnStartWhenBusy.integration.test.ts",
      "patchesUpstream": true
    },
    {
      "name": "fm-turn-start-when-busy-environment",
      "description": "A deferred turn start (whenBusy: \"queue\") can carry an environment. The engine keeps it in memory, never on an event or in the deferred-turn-start table, and hands it to the provider reactor when the deferred turn start runs, so the session spawned or restarted for it gets it, as for a direct start. The deferred turn start records only hasEnvironment. After a restart the environment is gone, so the decider drops it with the environment-lost reason instead of running it without it. The decider asks the engine through DeciderContext, a signed-off exception to pure orchestration. The server advertises turnStartWhenBusyEnvironment in its environment capabilities.",
      "keep": "TurnEnvironment.ts, DeciderContext.ts and deferredTurnStarts.ts are ours whole, including makeDeferredTurnEnvironments and the environment-lost drop in releaseDeferredTurnStart. The hasEnvironment field and the environment-lost drop reason survive in orchestration.ts; the turnStartWhenBusyEnvironment capability survives in environment.ts and ServerEnvironment.ts; and the deferredTurnEnvironments instance, the DeciderContext it provides around decideOrchestrationCommand and its remember call after the commit survive in OrchestrationEngine.ts. Everything else in those files takes upstream.",
      "files": [
        "packages/contracts/src/orchestration.ts",
        "packages/contracts/src/environment.ts",
        "apps/server/src/environment/ServerEnvironment.ts",
        "apps/server/src/orchestration/TurnEnvironment.ts",
        "apps/server/src/orchestration/DeciderContext.ts",
        "apps/server/src/orchestration/deferredTurnStarts.ts",
        "apps/server/src/orchestration/Layers/OrchestrationEngine.ts"
      ],
      "test": "apps/server/src/orchestration/Layers/OrchestrationEngine.test.ts",
      "testName": "defers a turn start that carries an environment and starts it when free|after a restart, drops a deferred turn start whose environment died with the process|does not defer a turn start that carries an environment without whenBusy",
      "patchesUpstream": true
    },
    {
      "name": "fm-turn-start-when-busy-environment-reactor-tests",
      "description": "The provider reactor tests for fm-turn-start-when-busy-environment: a deferred turn start spawns or restarts its session with its environment, and one on an idle thread applies it like a direct start. The manifest takes one test file per entry, so these tests have their own entry.",
      "keep": "The three tests named in testName, the setThreadTurnRunning helper and the whenBusy option of dispatchTurn survive in ProviderCommandReactor.test.ts. Everything else in that file takes upstream.",
      "files": ["apps/server/src/orchestration/Layers/ProviderCommandReactor.test.ts"],
      "test": "apps/server/src/orchestration/Layers/ProviderCommandReactor.test.ts",
      "testName": "a deferred turn start spawns its session with its environment|a deferred turn start restarts a live session for its environment|a turn start that asks to wait applies its environment on an idle thread",
      "patchesUpstream": true
    },
    {
      "name": "fm-turn-start-when-busy-environment-restart-test",
      "description": "The startup test for fm-turn-start-when-busy-environment: after a real restart a deferred turn start with an environment is dropped with environment-lost and the one behind it runs. The manifest takes one test file per entry, so this test has its own entry.",
      "keep": "The test named in testName and the decodeProviderEnvironment helper survive in orphanedProviderSessionStartup.integration.test.ts. Everything else in that file takes upstream.",
      "files": ["apps/server/integration/orphanedProviderSessionStartup.integration.test.ts"],
      "test": "apps/server/integration/orphanedProviderSessionStartup.integration.test.ts",
      "testName": "drops a deferred turn start whose environment a restart lost",
      "patchesUpstream": true
    },
    {
      "name": "fm-turn-start-when-busy-ended-session",
      "description": "A thread whose session ended (stopped, interrupted or error) counts as free for a turn start that asks to queue once no turn is active and the latest turn is not running, so the start runs at once instead of becoming a deferred turn start that no event releases. A runtime error without a turn id keeps the running turn named in the session, so an error raised mid-turn keeps the thread busy while an error left by a failed turn frees it. A deferred turn start an older server left on such a thread runs at the next startup, through the startup release loop.",
      "keep": "deferredTurnStarts.ts is ours whole, including the ended-session rule in sessionIsFree. In ProviderRuntimeIngestion.ts the runtime.error session-set keeps activeTurnId: eventTurnId ?? activeTurnId; everything else in that file takes upstream.",
      "files": [
        "apps/server/src/orchestration/deferredTurnStarts.ts",
        "apps/server/src/orchestration/Layers/ProviderRuntimeIngestion.ts"
      ],
      "test": "apps/server/integration/turnStartWhenBusy.integration.test.ts",
      "testName": "a turn start that asks to queue runs at once on a thread whose session stopped|a turn start that asks to queue runs at once after a failed turn|a turn start that asks to queue waits for a turn that raised a runtime error without a turn id",
      "patchesUpstream": true
    },
    {
      "name": "fm-turn-start-when-busy-ended-session-restart-test",
      "description": "The startup test for fm-turn-start-when-busy-ended-session: a deferred turn start left on a stopped session before a restart runs after it. A testName narrows a single test file, and this test lives in another file, so it has its own entry.",
      "keep": "The test named in testName survives in orphanedProviderSessionStartup.integration.test.ts. Everything else in that file takes upstream.",
      "files": ["apps/server/integration/orphanedProviderSessionStartup.integration.test.ts"],
      "test": "apps/server/integration/orphanedProviderSessionStartup.integration.test.ts",
      "testName": "starts a turn start deferred on a stopped session after a restart",
      "patchesUpstream": true
    },
    {
      "name": "claude-option-change-keeps-background-work",
      "description": "A Claude model or option change, such as the context window, no longer restarts a session that has live background work. A restart ends every background shell and agent the Claude process runs. The reactor keeps that session, and the adapter applies the change to it with setModel, applyFlagSettings and setMaxThinkingTokens. With no background work the change still restarts, as upstream does.",
      "keep": "The backgroundLiveness condition on shouldRestartForModelSelectionChange survives in ProviderCommandReactor.ts. In ClaudeAdapter.ts these survive: ClaudeFlagSettings and its Query import, ClaudeSelectionSettings, flagOrCleared, resolveClaudeSelectionSettings, launchThinkingDisplay, applySelectionSettings, the selectionSettings context field in place of currentEffort (and the subagent effort that reads it), the applyFlagSettings member and the thinkingDisplay parameter of setMaxThinkingTokens in ClaudeQueryRuntime, and their use in startSession and sendTurn. In ProviderCommandReactor.test.ts these survive: the two tests named in testName, the backgroundLiveness harness field, ThreadBackgroundLivenessService in the runtime type, and Layer.provideMerge(ThreadBackgroundLiveness.layer) in projectionSnapshotLayer. Everything else in those files takes upstream.",
      "files": [
        "apps/server/src/orchestration/Layers/ProviderCommandReactor.ts",
        "apps/server/src/provider/Layers/ClaudeAdapter.ts",
        "apps/server/src/orchestration/Layers/ProviderCommandReactor.test.ts"
      ],
      "test": "apps/server/src/orchestration/Layers/ProviderCommandReactor.test.ts",
      "testName": "keeps a claude session with live background work when only the context window changes|still restarts a session with live background work when the instance changes",
      "patchesUpstream": true
    },
    {
      "name": "claude-option-change-keeps-background-work-adapter-tests",
      "description": "The Claude adapter tests for claude-option-change-keeps-background-work: an option change reaches the running query without a new one. The manifest takes one test file per entry, so these tests have their own entry.",
      "keep": "The two tests named in testName and the applyFlagSettingsCalls field and applyFlagSettings member of FakeClaudeQuery survive in ClaudeAdapter.test.ts. Everything else in that file takes upstream.",
      "files": ["apps/server/src/provider/Layers/ClaudeAdapter.test.ts"],
      "test": "apps/server/src/provider/Layers/ClaudeAdapter.test.ts",
      "testName": "applies option changes to the running session without a new query|turns thinking off and on in the running session",
      "patchesUpstream": true
    },
    {
      "name": "live-background-work-restart-warning",
      "description": "Picking a model on another provider instance while the thread has live background work shows a warning toast: the switch restarts the session on the next message and the background work stops. Web and desktop only; the mobile picker cannot switch instance in a started thread.",
      "keep": "getLiveBackgroundWorkRestartWarning and its OrchestrationThreadShell import survive in ChatView.logic.ts, its describe block and import survive in ChatView.logic.test.ts, and its import, its toast in onProviderModelSelect and the activeThreadShell?.backgroundLiveness dependency of that callback survive in ChatView.tsx. Everything else in those files takes upstream.",
      "files": [
        "apps/web/src/components/ChatView.logic.ts",
        "apps/web/src/components/ChatView.tsx",
        "apps/web/src/components/ChatView.logic.test.ts"
      ],
      "test": "apps/web/src/components/ChatView.logic.test.ts",
      "testName": "getLiveBackgroundWorkRestartWarning",
      "patchesUpstream": true
    },
    {
      "name": "stopped-background-command-rows",
      "description": "A resumed Claude session reports the previous process's unfinished background shells and monitors as stopped tasks with no task type. Ingestion recovers the type and owning agent from the task's saved start row, so they are no longer stamped as subagents. Web and mobile label a stopped background command with its description and fold several that stopped together into one row. backgroundCommands.ts is the fork's own module and upstream has no file at that path.",
      "keep": "The taskType and agentId recovery from the saved task row (findTaskClassInActivities and its call in the task.completed branch) survives in ProviderRuntimeIngestion.ts, and its test survives in ProviderRuntimeIngestion.test.ts. Everything else in those files takes upstream.",
      "files": [
        "apps/server/src/orchestration/Layers/ProviderRuntimeIngestion.ts",
        "apps/server/src/orchestration/Layers/ProviderRuntimeIngestion.test.ts"
      ],
      "test": "apps/server/src/orchestration/Layers/ProviderRuntimeIngestion.test.ts",
      "testName": "classifies a resumed session",
      "patchesUpstream": true
    },
    {
      "name": "stopped-background-command-rows-web",
      "description": "The web half of stopped-background-command-rows: the row label and the fold of adjacent stopped background commands.",
      "keep": "backgroundCommands.ts is ours whole, and so is its ./work-log/background-commands export in packages/client-runtime/package.json. In session-logic.ts the background-commands import, the stoppedBackgroundCommands field, its label in toDerivedWorkLogEntry and its fold in collapseDerivedWorkLogEntries survive, and the \"stopped background commands\" describe block survives in session-logic.test.ts. Everything else in those files takes upstream.",
      "files": [
        "packages/client-runtime/src/work-log/backgroundCommands.ts",
        "packages/client-runtime/package.json",
        "apps/web/src/session-logic.ts",
        "apps/web/src/session-logic.test.ts"
      ],
      "test": "apps/web/src/session-logic.test.ts",
      "testName": "stopped background commands",
      "patchesUpstream": true
    },
    {
      "name": "stopped-background-command-rows-mobile",
      "description": "The mobile half of stopped-background-command-rows, on the same shared module as web.",
      "keep": "In threadActivity.ts the background-commands import, the stoppedBackgroundCommands field, its label in toDerivedWorkLogEntry and its fold in collapseDerivedWorkLogEntries survive, and the \"stopped background commands\" describe block survives in threadActivity.test.ts. Everything else in those files takes upstream.",
      "files": [
        "apps/mobile/src/lib/threadActivity.ts",
        "apps/mobile/src/lib/threadActivity.test.ts"
      ],
      "test": "apps/mobile/src/lib/threadActivity.test.ts",
      "testName": "stopped background commands",
      "patchesUpstream": true
    },
    {
      "name": "fork-app-icon",
      "description": "The fork's app icon: an orange T3 tile with a navy corner badge carrying the badge label from forkBuild.ts. forkAppIcon.ts holds its geometry once; the sidebar header renders it as SVG and scripts/generate-fork-icons.ts rasterises it into every icon file the desktop and web builds ship, so the taskbar icon and the header icon match. forkAppIcon.ts, ForkAppIcon.tsx, generate-fork-icons.ts and assets/fork/ are the fork's own and upstream has none of them, so the tests cannot pass on plain upstream. Issues #59 and #154.",
      "keep": "forkAppIcon.ts, ForkAppIcon.tsx, generate-fork-icons.ts and every file in assets/fork/ are ours whole. The ./forkAppIcon export survives in packages/shared/package.json, the fork* icon paths and the fork web brand survive in brand-assets.ts, and SidebarChrome.tsx still shows ForkAppIcon in the sidebar header. Everything else in those three files takes upstream. After any merge, run node scripts/generate-fork-icons.ts; the generator test fails if the committed icons drift from it.",
      "files": [
        "packages/shared/src/forkAppIcon.ts",
        "packages/shared/src/forkBuild.ts",
        "packages/shared/package.json",
        "apps/web/src/components/ForkAppIcon.tsx",
        "apps/web/src/components/sidebar/SidebarChrome.tsx",
        "scripts/generate-fork-icons.ts",
        "scripts/lib/brand-assets.ts",
        "assets/fork/fork-universal-1024.png",
        "assets/fork/fork-macos-1024.png",
        "assets/fork/fork-windows.ico",
        "assets/fork/fork-web-favicon.ico",
        "assets/fork/fork-web-favicon-16x16.png",
        "assets/fork/fork-web-favicon-32x32.png",
        "assets/fork/fork-web-apple-touch-180.png"
      ],
      "test": ["packages/shared/src/forkAppIcon.test.ts", "scripts/generate-fork-icons.test.ts"],
      "patchesUpstream": true
    }
  ]
}
